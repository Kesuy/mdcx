## 新功能
- 在“设置 → 下载”新增“优先使用同番号本地图片”开关；刮削时会识别原影片目录中包含影片番号的 JPG、JPEG、PNG 和 WebP 图片，包括以 `.` 开头的隐藏图片。
- 同番号图片会全部移动到刮削完成目录；多张图片严格按文件名排序，并使用第一张生成封面图（Poster）、缩略图（Thumb）和背景图（Fanart），不再从网站重复下载这些图片。

## 优化
- 本地图片继续使用原有封面裁切、直接复制和水印设置；PNG、WebP 等来源生成刮削图片时会转换为标准 JPEG，避免文件扩展名与实际格式不一致。
- Release 说明改为中文“新功能 / 优化 / 修复”分类，并自动省略没有内容的分类，不再直接展示 commit SHA 和英文提交标题。
