## 新功能
- 封面裁切窗口新增“保持比例”，并提供默认、原比例、2:3、3:2、16:9 和 9:16 预设；宽高调整会在图片边界内自动联动。
- 封面裁切支持连续左旋、右旋 90°，预览、裁切区域以及 Poster、Thumb、Fanart 会使用相同的累计旋转方向。

## 优化
- 版本号从旧日期格式迁移到以 `3.0` 开始的点分数字格式；更新检查、构建、版本提升和发布流程仍兼容旧版升级比较。
- 调整封面裁切窗口布局，完整展示比例、旋转、裁切范围和水印控制，在不同缩放与图片方向下更易操作。

## 修复
- 修复旋转后预览与部分输出图片方向不一致的问题；当源图本身是 Thumb 或 Fanart 时，也会安全更新同路径图片。
- 同路径旋转保存改用保留文件权限的原子替换，写入失败时不会破坏源图或遗留临时文件。
- 修复新旧版本 Tag 过渡时 changelog 起点和 Release 附件身份可能不准确的问题。
