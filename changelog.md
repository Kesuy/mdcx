## 新功能
- 主界面新增“打开 NFO 文件”按钮：选择本地 `*.nfo` 后，自动关联同目录视频、NFO、`poster.jpg`、`fanart.jpg`、`thumb.jpg` 等文件，并填充番号、演员、标题、简介和图片预览。
- 支持同番号多 CD 影片加载与编辑：保存 CD1/CD2 任一分集的 NFO 后，同组分集元数据会同步更新，并按当前命名设置整体重新整理目录名、视频、NFO、字幕和关联素材。
- 主界面成功列表新增“完成顺序 / 番号 / 演员”排序，支持升序和降序，并保留当前选中影片及详情。
- 在主界面编辑 NFO 信息并保存后，自动按当前目录和文件命名设置重新整理已刮削影片；视频、NFO 及同名前缀关联文件同步重命名，固定名称图片素材保持不变。
- 新增响应式布局支持：窗口最小尺寸调整为 1030×650，修复小窗口下元数据控件、设置页、日志区域等的裁切和重叠问题。

## 优化
- 优化 FlareSolverr 请求协议：正确传递代理配置，自动处理软件代理与 FlareSolverr 返回的 Cookie/User-Agent，并在代理兼容问题时安全回退。
- 优化长番号和演员文本显示：完整值保留在 tooltip 中，界面按可用宽度省略，避免挤压其他控件。
- 自动同步主界面文件路径、图片路径、成功记录和多 CD 缓存，并清理空的旧目录。

## 修复
- 修复编辑后多 CD 目录因“包含多个影片文件”被拒绝的问题；现在只接受同番号、分集后缀明确的安全分组，混合番号或不明确目录仍拒绝整理。
- 修复 Windows 保留名（如 `CON`、`COM1.txt`）在 Linux/Docker 上生成时未被规避的问题，保证输出目录可在 Windows/SMB 上使用。
- 修复 FlareSolverr 场景下代理、Cookie 和响应状态处理不一致的问题。
- 修复 `H4610`、`H0930` 等番号及长番号显示/识别相关问题。

## 安全策略
- 目标路径冲突、目录混有其他番号、分集标记不完整或任一文件无法安全移动时，不执行整理并保留实际路径。
- 多文件重命名采用预检查和回滚，避免只移动部分 CD 或覆盖已有文件。
