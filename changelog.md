## 4.0.5

### 修复
- 结果控件会在替换 Designer 旧树后再连接信号，并按鼠标点击的模型索引直接解析绑定数据，修复真实主窗口中高亮条目与详情内容不一致。
- 清理文件大小规则关闭时不再强制填写阈值；启用后仍进行非负数字校验并行内标红。
- 修复工具页进入后滚动位置异常、网络设置切换或滚动后被内容撑宽，以及工具表单两侧留白过大；工具页底色、分组卡片、标签列、输入框及主次按钮与设置页统一。
- 网络页增加动态宽度钳制，工具页表单按设置页边距随可视区伸缩；窗口最小尺寸调整为完整布局安全基线 1089×700，禁止缩入会触发布局错乱的窄屏尺寸。
- 侧栏与内容区共用无缝外壳背景和主题化不透明 1px 分隔线，显式清除侧栏内侧圆角；顶部进度条限制在内容区内，消除浅色、深色桌面下交界处上下露底和错位。
- 输入框、下拉框、数字框和多行编辑器统一为 8px 圆角矩形。

### 优化
- FC2 字段优先模式仅在 fc2cmadb 成功且未启用预告片下载时跳过空预告片回退；启用预告片时仍遵循原有站点优先级继续补全。

### 工程
- Windows EXE 现在自动写入文件说明、产品名称、文件/产品版本、版权、内部名称、原始文件名、项目地址和简体中文语言信息。
- PE 文件属性直接读取 `LOCAL_VERSION` 生成；构建完成后会反向读取 EXE 校验，版本或详情缺失时禁止发布。
- pytest 缓存目录固定到可重建的 `.cache/pytest`；历史 `.pytest-cache` / `.test-cache` 即使存在异常 ACL 也不会再污染后续全量测试。
