## 4.0.7

### 修复
- 修复工具页首次打开时内容宽度没有同步到可视区域、两侧留白过大，以及分组首行越出卡片背景；切换页面后会立即重新同步布局，不再依赖手动调整窗口。
- “输入网址重新刮削”现在会强制刷新封面、缩略图、背景图和剧照，避免影片信息已更新但仍显示上一次刮削图片；下载或替换失败时会原子保留原图片。
- “显示高级设置”状态写入当前配置，下次启动或切换配置后保持用户选择，并兼容缺少该字段的旧配置。
- 修正同番号本地图片的匹配、转码和整理路径，失败时不会提前移动或丢失原图。
- 修正封面裁切窗口旋转后的像素方向及图片更新路径。

### 界面与架构
- 设置页 12 个标签、60 个分组全部改为 Designer 原生 Qt Layout，移除运行时 geometry 迁移层；首次显示、缩放、滚动和 DPI 变化均由布局系统管理。
- 主界面、日志、工具、设置、网络、说明和 NFO 浮层拆分为独立视图组件，保留原有 objectName、信号和配置兼容。
- 清除全部 Designer 内联 QSS，导航、表单、状态、Cookie 编辑器和明暗主题统一使用语义主题 token。
- 主窗口从约 2846 行、221 个方法收敛为 282 行、13 个装配及兼容薄槽；页面初始化、结果交互、设置/工具、生命周期、预览、日志和帮助分别进入独立控制器。
- NFO、字段规则、Emby、水印位置和全局开关迁移到可测试的复合配置 schema，网站有序优先级继续使用拖放编辑器。

### 依赖与工程
- 升级 lxml、Pillow、pydantic-settings、PyInstaller 和 pytest，并同步锁文件，处理已知依赖安全告警。
- 增加 UI 架构、原生布局、配置往返、重新刮削图片和本地图片整理回归测试；UI 生成脚本会阻止内联 QSS 或巨型界面回退。
- 修复发布说明生成器无法识别复合中文分类、导致跨平台产物构建成功后未上传的问题。
