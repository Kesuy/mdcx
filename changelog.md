## 4.0.8

### 修复
- 修复部分刮削任务因新旧运行状态断开而立即结束，以及错误处理路径中的 `copy` 局部变量异常；保留旧 `Flags` 工作流兼容并由单次任务 session 统一跟踪进度、剩余任务和失败记录。
- 修复 `FZ65` / `FZ-65`、`FDD2007` / `FDD-2007` 及文件名带“无码”等附加文本时的番号识别，JavBus 与 AVSOX 搜索可兼容有无连字符的写法。
- 修复演员为空、4K 标记等情况下命名模板字段被错误清理，恢复按用户配置整理目录和文件名。
- 单个 NFO 保存不再弹出重复确认；设置页右键标准菜单恢复中文。

### 界面
- 修复设置页网站偏好缺失、下载/命名/刮削目录等页面错位、控件裁切和滚动范围异常，并覆盖 880 至 1440 像素的响应式宽度。
- 普通输入框、标签、按钮、下拉框、对话框和弹出控件统一继承当前系统可用的中文 UI 字体，不再在设置页混用等宽字体。
- 修复设置页首次打开误报“有 1 项未保存”，完善搜索定位、高亮、未保存计数及撤销当前更改。
- 结果列表补充阶段与来源信息，并提供可展开的结构化失败详情和可重试入口。

### 数据与工程
- 新增字段来源记录，覆盖标题、演员、制作商、导演、标签、简介及主要图片字段，不改变原有元数据持久化格式。
- 增加 Windows 轻量 CI、主窗口/刮削真实入口回归测试、设置页多宽度布局检查及冻结 EXE smoke test。
- 调整 Release 流程为测试和双平台构建全部成功后创建草稿、上传并校验完整附件，最后才公开发布。
